---
phase: 05-observability-hardening-and-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/schemas/src/__tests__/card-analysis.test.ts
  - packages/schemas/src/__tests__/portfolio-summary.test.ts
  - packages/schemas/src/__tests__/archetype.test.ts
  - packages/schemas/src/__tests__/action.test.ts
  - packages/schemas/vitest.config.ts
  - packages/agent/src/rules/__tests__/gap-fill.test.ts
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "All Zod schemas reject invalid inputs with meaningful error messages"
    - "All Zod schemas accept valid inputs matching the defined types"
    - "Rules engine tests cover every card state and action type combination"
  artifacts:
    - path: "packages/schemas/src/__tests__/card-analysis.test.ts"
      provides: "CardAnalysis schema validation tests"
      contains: "describe.*CardAnalysis"
    - path: "packages/schemas/src/__tests__/portfolio-summary.test.ts"
      provides: "PortfolioSummary schema validation tests"
      contains: "describe.*PortfolioSummary"
    - path: "packages/schemas/src/__tests__/archetype.test.ts"
      provides: "CollectorArchetype schema validation tests"
      contains: "describe.*Archetype"
    - path: "packages/schemas/src/__tests__/action.test.ts"
      provides: "Action schema validation tests"
      contains: "describe.*Action"
  key_links:
    - from: "packages/schemas/src/__tests__/*.test.ts"
      to: "packages/schemas/src/index.ts"
      via: "import schemas for validation"
      pattern: "import.*from.*@tcg/schemas"
---

<objective>
Add comprehensive Zod schema validation tests (valid + invalid inputs) for all schema types, and gap-fill rules engine test coverage to ensure every card state x action type is tested.

Purpose: Ensure all data contracts are tested against both happy and unhappy paths, and the rules engine has complete coverage.
Output: Schema test files with valid/invalid input cases, gap-fill rules engine tests.
</objective>

<execution_context>
@/Users/chaseguo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaseguo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-observability-hardening-and-testing/05-CONTEXT.md

@packages/schemas/src/index.ts
@packages/schemas/src/llm/card-analysis.ts
@packages/schemas/src/llm/portfolio-summary.ts
@packages/schemas/src/llm/archetype.ts
@packages/schemas/src/llm/action.ts
@packages/schemas/src/api/card-analysis.ts
@packages/schemas/src/api/portfolio-summary.ts
@packages/schemas/src/api/archetype.ts
@packages/schemas/src/__tests__/card-analysis.test.ts
@packages/schemas/src/__tests__/portfolio-summary.test.ts
@packages/schemas/src/__tests__/archetype.test.ts
@packages/schemas/src/__tests__/action.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Comprehensive Zod schema validation tests</name>
  <files>packages/schemas/src/__tests__/card-analysis.test.ts, packages/schemas/src/__tests__/portfolio-summary.test.ts, packages/schemas/src/__tests__/archetype.test.ts, packages/schemas/src/__tests__/action.test.ts, packages/schemas/vitest.config.ts</files>
  <behavior>
    - Valid CardAnalysis with all required fields parses successfully
    - CardAnalysis with missing required fields (identity_tags, rarity_signal, etc.) fails parsing
    - CardAnalysis with wrong types (string where number expected) fails parsing
    - CardAnalysis with null values on nullable fields parses successfully (LLM compat)
    - Valid PortfolioSummary with breakdown, scores, recommended_actions parses
    - PortfolioSummary with invalid concentration_score (out of range or wrong type) fails
    - Valid CollectorArchetype with name, traits, why, comparable_collectors, share_card_text, share_card_badges parses
    - CollectorArchetype with missing traits or badges fails
    - Valid Action with type, params, ui_copy, risk_notes parses
    - Action with invalid action_type enum value fails
    - API response schemas (CardAnalysisResponse, PortfolioSummaryResponse, ArchetypeResponse) accept valid extended shapes
    - API response schemas reject payloads missing the actions field (which API adds but LLM schema lacks)
  </behavior>
  <action>
    1. Read existing test files in packages/schemas/src/__tests__/ to understand current coverage and patterns. The 4 test files already exist -- extend them rather than rewriting.

    2. For each schema (CardAnalysis, PortfolioSummary, CollectorArchetype, Action), ensure tests cover:
       - Happy path: valid object with all fields parses to correct type
       - Missing required fields: each required field omitted individually causes parse failure
       - Wrong types: number in string field, string in array field, etc.
       - Nullable fields: null value accepted where z.nullable() is used (critical for LLM compat per Phase 1 decision)
       - Enum validation: invalid enum values rejected (CardState, PriceConfidence, ActionType)

    3. For API response schemas (CardAnalysisResponse, PortfolioSummaryResponse, ArchetypeResponse), add tests verifying:
       - Valid full response shapes parse
       - The actions field (injected by rules engine, not LLM) is required in API schemas
       - The degraded field is optional

    4. Ensure packages/schemas/vitest.config.ts exists and is correctly configured. If it already exists, leave it. If not, create it matching the pattern from packages/agent/vitest.config.ts.

    5. Use `safeParse` for invalid input tests so failures are assertions on `.success === false` and `.error.issues`, not thrown exceptions.
  </action>
  <verify>
    <automated>pnpm --filter @tcg/schemas test --run</automated>
  </verify>
  <done>All Zod schemas have tests for valid inputs, missing required fields, wrong types, nullable fields, and enum validation. Both LLM-facing and API-facing schemas covered. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rules engine test gap-fill</name>
  <files>packages/agent/src/rules/__tests__/gap-fill.test.ts</files>
  <action>
    1. Read existing rules engine tests to identify current coverage. Phase 2 created 89 tests covering all states x actions. CONTEXT.md notes this "may only need gap-fill."

    2. Run the existing tests and check coverage: `pnpm --filter @tcg/agent test --run -- --reporter=verbose 2>&1 | grep -E "(PASS|FAIL|✓|×|test)" | head -60` to understand which state x action combinations are already tested.

    3. Identify gaps by checking every combination of:
       - States: VAULTED, EXTERNAL, ON_MARKET, IN_TRANSIT
       - Actions: BUYBACK, LIST, REDEEM, SHIP_TO_VAULT, OPEN_PACK, WATCHLIST, BUNDLE_SHIP
       - Edge cases: NO_DATA priceConfidence, STALE_7D risk_notes, null estimatedValue

    4. Create gap-fill.test.ts with any missing combinations. If all combinations are already covered, create a small file that imports and re-validates the critical edge cases documented in STATE.md decisions:
       - BUYBACK hidden (not just suppressed) for NO_DATA
       - STALE_7D adds risk_notes but does not suppress BUYBACK/LIST
       - ON_MARKET returns [] (no actions)
       - IN_TRANSIT returns WATCHLIST only
       - batchEligible computed before single-card loop

    5. If gap-fill reveals zero gaps, create the test file with a single describe block documenting that coverage is complete and add a "coverage sentinel" test that verifies the exhaustive switch still covers all CardState values.
  </action>
  <verify>
    <automated>pnpm --filter @tcg/agent test --run</automated>
  </verify>
  <done>Rules engine test coverage is verified complete for all card states x action eligibility combinations. Gap-fill tests pass alongside existing 89 tests. TEST-02 requirement satisfied.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @tcg/schemas test --run` passes all schema tests
2. `pnpm --filter @tcg/agent test --run` passes all rules + agent tests
3. `pnpm test` passes across entire monorepo
</verification>

<success_criteria>
- TEST-01: All Zod schemas tested with valid and invalid inputs, nullable fields, enum validation
- TEST-02: Rules engine has verified complete coverage of all card states x action types
- No test regressions in existing suite
</success_criteria>

<output>
After completion, create `.planning/phases/05-observability-hardening-and-testing/05-02-SUMMARY.md`
</output>
