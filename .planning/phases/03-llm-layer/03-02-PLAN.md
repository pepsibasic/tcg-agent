---
phase: 03-llm-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/agent/src/llm/prompts.ts
  - packages/agent/src/llm/compliance.ts
  - packages/agent/src/llm/__tests__/prompts.test.ts
  - packages/agent/src/llm/__tests__/compliance.test.ts
autonomous: true
requirements: [LLM-02, LLM-04]

must_haves:
  truths:
    - "Prompt templates for card narrative, portfolio summary, and archetype identity are registered and renderable with slot injection"
    - "Any LLM output containing financial advice language is detected and replaced before reaching the API response"
    - "Compliance scrubs are logged with field name, original text, replacement text"
  artifacts:
    - path: "packages/agent/src/llm/prompts.ts"
      provides: "Prompt template registry with slot injection rendering"
      exports: ["renderPrompt", "PROMPTS"]
    - path: "packages/agent/src/llm/compliance.ts"
      provides: "Compliance guard that scrubs financial advice language from LLM outputs"
      exports: ["scrubCompliance", "ComplianceScrubResult"]
  key_links:
    - from: "packages/agent/src/llm/compliance.ts"
      to: "LLM output fields"
      via: "scans reasoning_bullets, rarity_signal, liquidity_signal, recommendedActions, why, share_card_text"
      pattern: "scrubCompliance"
    - from: "packages/agent/src/llm/prompts.ts"
      to: "packages/agent/src/llm/client.ts (Plan 01)"
      via: "rendered prompt string passed to generateStructured"
      pattern: "renderPrompt.*PROMPTS"
---

<objective>
Create prompt templates with slot injection and the compliance guard that scrubs financial advice language.

Purpose: Prompt templates define the LLM's output personality and structure. The compliance guard is the safety net ensuring no financial advice leaks through, regardless of what the LLM produces.
Output: Prompt registry with 3 templates (card, portfolio, archetype), compliance scrub function with blocklist patterns, full test coverage.
</objective>

<execution_context>
@/Users/chaseguo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaseguo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-llm-layer/03-CONTEXT.md

<interfaces>
<!-- Schemas whose fields the compliance guard must scan -->

CardAnalysis narrative fields to scan: rarity_signal, liquidity_signal, reasoning_bullets
PortfolioSummary narrative fields to scan: recommendedActions
CollectorArchetype narrative fields to scan: why, share_card_text

Fields to SKIP (not narrative): identity_tags, card_id, numeric fields, badges, traits, comparable_collectors
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prompt templates and compliance guard</name>
  <files>packages/agent/src/llm/prompts.ts, packages/agent/src/llm/compliance.ts</files>
  <behavior>
    - renderPrompt('card_analysis', { card_name: 'Charizard', ... }) returns a string with slots filled
    - renderPrompt with missing required slot throws descriptive error
    - PROMPTS registry has keys: card_analysis, portfolio_summary, archetype_identity
    - Each template starts with system persona: 'You are a knowledgeable card market analyst...'
    - scrubCompliance detects 'will increase in value' and replaces with 'shows positive market signals'
    - scrubCompliance detects 'guaranteed return', 'buy now', 'sell immediately', 'investment advice', 'should buy/sell/hold', 'profit'
    - scrubCompliance returns { scrubbed: string, violations: Array<{ field, original, replacement }> }
    - scrubCompliance on clean text returns original with empty violations array
    - scrubCompliance scans only specified narrative fields, not numeric/id fields
  </behavior>
  <action>
  1. Create `packages/agent/src/llm/prompts.ts`:
     - Define `PromptTemplate` type: `{ system: string, user: string, requiredSlots: string[] }`
     - Define `SYSTEM_PERSONA` constant: 'You are a knowledgeable card market analyst. Be direct, data-informed, and engaging. Never give financial advice. Use signals language. When a field value is unknown, return null (not omit the field).'
     - Define `PROMPTS` registry as `Record<string, PromptTemplate>`:
       - `card_analysis`: System = SYSTEM_PERSONA. User template with slots: {{card_name}}, {{card_set}}, {{card_grade}}, {{card_state}}, {{estimated_value}}, {{price_confidence}}, {{user_notes}}. Instructs LLM to produce CardAnalysis fields: identity_tags, rarity_signal, liquidity_signal, price_band, reasoning_bullets, confidence. Style: analytical with personality per CONTEXT.md decisions.
       - `portfolio_summary`: System = SYSTEM_PERSONA. User template with slots: {{user_id}}, {{cards_json}}, {{total_count}}, {{vaulted_count}}, {{external_count}}. Instructs LLM to produce breakdown, concentration/liquidity scores, recommended actions. Style: key insights + recommendations per CONTEXT.md.
       - `archetype_identity`: System = SYSTEM_PERSONA. User template with slots: {{user_id}}, {{portfolio_summary_json}}, {{top_ips}}, {{action_history}}. Instructs LLM to produce name, traits, why, comparable_collectors, share_card_text, badges. Style: fun and shareable per CONTEXT.md.
     - `function renderPrompt(templateKey: string, slots: Record<string, string>): { system: string, user: string }` — looks up template, validates all requiredSlots present (throws if missing), replaces `{{slot}}` patterns with values, returns rendered system + user strings.
     - Export `renderPrompt`, `PROMPTS`, `SYSTEM_PERSONA`

  2. Create `packages/agent/src/llm/compliance.ts`:
     - Define `ComplianceViolation` type: `{ field: string, original: string, replacement: string }`
     - Define `ComplianceScrubResult` type: `{ text: string, violations: ComplianceViolation[] }`
     - Define `BLOCKLIST` as array of `{ pattern: RegExp, replacement: string }`:
       - 'will increase( in value)?' -> 'shows positive market signals'
       - 'guaranteed (return|profit|gain)' -> 'historical performance suggests'
       - 'buy now' -> 'consider acquiring'
       - 'sell immediately' -> 'consider listing'
       - 'should (buy|sell|hold)' -> 'you might consider'
       - 'investment advice' -> 'market observations'
       - 'profit' -> 'potential value'
       - 'guaranteed' -> 'historically consistent'
       - 'will (definitely|certainly|surely)' -> 'may potentially'
       - All patterns case-insensitive with word boundaries where appropriate
     - `function scrubText(text: string, field: string): ComplianceScrubResult` — applies all blocklist patterns, collects violations
     - `function scrubCompliance<T extends Record<string, unknown>>(data: T, narrativeFields: string[]): { data: T, violations: ComplianceViolation[] }` — iterates only over `narrativeFields` keys, applies `scrubText` to string values and string array elements, returns modified data + all violations. Skips non-string fields. For array fields (like reasoning_bullets), scrubs each element.
     - Export `scrubCompliance`, `ComplianceScrubResult`, `ComplianceViolation`, `scrubText`

  3. Ensure `pnpm build` passes.
  </action>
  <verify>
    <automated>cd /Users/chaseguo/Documents/GitHub/tcg-agent && pnpm build && pnpm --filter '*agent*' test</automated>
  </verify>
  <done>Prompt templates registered for all 3 types with slot injection working, compliance guard detects and replaces all blocklisted patterns, build passes</done>
</task>

<task type="auto">
  <name>Task 2: Comprehensive tests for prompts and compliance</name>
  <files>packages/agent/src/llm/__tests__/prompts.test.ts, packages/agent/src/llm/__tests__/compliance.test.ts</files>
  <action>
  1. Create `packages/agent/src/llm/__tests__/prompts.test.ts`:
     - Test PROMPTS has all 3 keys: card_analysis, portfolio_summary, archetype_identity
     - Test each template has system starting with SYSTEM_PERSONA
     - Test each template system contains 'Never give financial advice'
     - Test each template instructs null (not omit) for unknown fields
     - Test renderPrompt fills slots correctly
     - Test renderPrompt throws on missing required slot with descriptive message
     - Test renderPrompt with extra (unused) slots does not throw
     - Test renderPrompt with all slots for each of the 3 templates

  2. Create `packages/agent/src/llm/__tests__/compliance.test.ts`:
     - Test scrubText detects 'will increase in value' -> replacement
     - Test scrubText detects 'guaranteed return' -> replacement
     - Test scrubText detects 'buy now' -> replacement
     - Test scrubText detects 'sell immediately' -> replacement
     - Test scrubText detects 'should buy' / 'should sell' / 'should hold' -> replacement
     - Test scrubText detects 'investment advice' -> replacement
     - Test scrubText detects 'profit' -> replacement
     - Test scrubText on clean text returns original, empty violations
     - Test scrubText is case-insensitive
     - Test scrubCompliance scans only narrativeFields keys
     - Test scrubCompliance skips numeric fields
     - Test scrubCompliance handles array fields (reasoning_bullets with violations)
     - Test scrubCompliance returns all violations collected across fields
     - Test scrubCompliance with a full CardAnalysis-like object: pass narrative fields, verify id/tags untouched
  </action>
  <verify>
    <automated>cd /Users/chaseguo/Documents/GitHub/tcg-agent && pnpm --filter '*agent*' test</automated>
  </verify>
  <done>All prompt tests pass confirming slot injection and template structure, all compliance tests pass confirming blocklist detection and field-scoped scanning</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- `pnpm --filter '*agent*' test` passes all prompt and compliance tests
- renderPrompt produces valid strings for all 3 template types
- scrubCompliance catches all financial advice patterns in CONTEXT.md decisions
</verification>

<success_criteria>
- 3 prompt templates registered with system persona and slot injection
- Compliance guard detects and replaces all blocklisted financial advice patterns
- Compliance scrub returns violation log (field, original, replacement) for Phase 5 observability
- Only narrative fields are scanned; numeric/id fields untouched
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-llm-layer/03-02-SUMMARY.md`
</output>
