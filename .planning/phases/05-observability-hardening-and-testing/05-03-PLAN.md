---
phase: 05-observability-hardening-and-testing
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/api/src/__tests__/integration/journeys.test.ts
autonomous: true
requirements:
  - TEST-03

must_haves:
  truths:
    - "Pack pull triggers card analysis and returns CardAnalysis JSON with actions from rules engine"
    - "External card upload via POST /external-cards appears in portfolio summary"
    - "Portfolio summary request returns PortfolioSummary with vaulted and external cards"
    - "Archetype endpoint returns shareable CollectorArchetype with badges"
  artifacts:
    - path: "apps/api/src/__tests__/integration/journeys.test.ts"
      provides: "HTTP integration tests for all 4 MVP user journeys"
      min_lines: 150
      contains: "fastify.inject"
  key_links:
    - from: "apps/api/src/__tests__/integration/journeys.test.ts"
      to: "apps/api/src/server.ts"
      via: "fastify.inject for real HTTP pipeline"
      pattern: "inject.*method.*POST"
    - from: "apps/api/src/__tests__/integration/journeys.test.ts"
      to: "packages/agent/src/llm"
      via: "vi.mock for LLM layer"
      pattern: "vi\\.mock"
---

<objective>
Create integration tests that verify all four MVP user journeys end-to-end via HTTP, using fastify.inject with mocked LLM responses.

Purpose: Prove the full request pipeline works from HTTP request to JSON response for every core user journey, exercising real routing, validation, request ID propagation, and response shaping.
Output: Integration test file with 4 journey test suites that exercise the complete API.
</objective>

<execution_context>
@/Users/chaseguo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaseguo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-observability-hardening-and-testing/05-CONTEXT.md
@.planning/phases/05-observability-hardening-and-testing/05-01-SUMMARY.md

@apps/api/src/server.ts
@apps/api/src/routes/agent.ts
@apps/api/src/routes/external-cards.ts
@apps/api/src/routes/actions.ts
@apps/api/src/__tests__/routes/agent.test.ts
@apps/api/src/__tests__/routes/external-cards.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MVP user journey integration tests</name>
  <files>apps/api/src/__tests__/integration/journeys.test.ts</files>
  <behavior>
    Journey 1 — Pack pull to card analysis:
    - POST /agent/card/analyze-batch with pack_pull source and card IDs returns 200
    - Response contains CardAnalysis array with actions from rules engine (not LLM)
    - Response includes X-Request-Id header

    Journey 2 — External card upload to portfolio appearance:
    - POST /external-cards with title, set, grade, cert, estimatedValue returns 201
    - POST /agent/portfolio/summary for same user includes the external card in breakdown
    - External card has no Gacha economy actions (SHIP_TO_VAULT/WATCHLIST only)

    Journey 3 — Portfolio summary request:
    - POST /agent/portfolio/summary with X-User-Id returns 200
    - Response contains total_value_est, breakdown, concentration_score, liquidity_score, recommended_actions
    - Response includes both vaulted and external cards

    Journey 4 — Shareable archetype export:
    - POST /agent/archetype with X-User-Id returns 200
    - Response contains name, traits, why, comparable_collectors, share_card_text, share_card_badges
    - Badges are deterministic (computed after LLM, not by LLM)
  </behavior>
  <action>
    1. Create apps/api/src/__tests__/integration/ directory.

    2. Study the existing route test pattern in apps/api/src/__tests__/routes/agent.test.ts to understand how fastify.inject is used, how Prisma is mocked (vi.mock), and how orchestrators are mocked. Follow the same patterns.

    3. Create journeys.test.ts with a shared test setup:
       - Build a Fastify instance using the same configuration as server.ts (register all route plugins)
       - Mock the LLM layer (vi.mock on packages/agent orchestrators or the generateWithRetry function) to return canned responses that match the Zod schemas. Do NOT mock routes or Prisma at the route level -- mock at the orchestrator/LLM boundary so the test exercises the full route handler logic.
       - Mock Prisma for database calls (findMany, create, etc.) with realistic test fixtures matching seed data shapes.

    4. Journey 1 (pack pull -> card analysis):
       - POST /agent/card/analyze-batch with body `{ cardIds: ["uuid1", "uuid2"], source: "pack_pull" }` and header `X-User-Id: test-user-id`
       - Assert 200 status, response body is array of CardAnalysis objects
       - Assert each result has `actions` array (from rules engine)
       - Assert response has X-Request-Id header

    5. Journey 2 (external upload -> portfolio):
       - POST /external-cards with body `{ title: "Charizard PSA 10", set: "Base Set", grade: "10", certNumber: "12345678", estimatedValue: 500 }` and X-User-Id header
       - Assert 201 status with created external card
       - Then POST /agent/portfolio/summary with same X-User-Id
       - Assert 200 and response body includes the external card in the breakdown

    6. Journey 3 (portfolio summary):
       - POST /agent/portfolio/summary with X-User-Id header
       - Assert 200 status
       - Assert response contains total_value_est, breakdown (array), concentration_score, liquidity_score, recommended_actions
       - Assert X-Request-Id header present

    7. Journey 4 (archetype export):
       - POST /agent/archetype with X-User-Id header
       - Assert 200 status
       - Assert response contains name, traits, why, comparable_collectors, share_card_text, share_card_badges
       - Assert badges array contains deterministic badge values (vault_builder, ip_specialist, or external_collector based on mock data)

    8. Each journey test should verify X-Request-Id is present in the response, confirming the observability work from Plan 01 is wired through.
  </action>
  <verify>
    <automated>pnpm --filter @tcg/api test --run</automated>
  </verify>
  <done>All 4 MVP user journey tests pass via fastify.inject. Tests exercise real routing, request ID propagation, response shaping, and schema validation. TEST-03 requirement fully satisfied.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter @tcg/api test --run` passes all tests including new journey tests
2. `pnpm test` passes across entire monorepo
3. Journey tests exercise all 4 MVP paths: pack pull, external upload, portfolio summary, archetype export
</verification>

<success_criteria>
- TEST-03: All 4 MVP user journeys tested end-to-end via HTTP
- Each journey test verifies correct HTTP status, response schema shape, and X-Request-Id header
- LLM mocked at orchestrator boundary -- tests exercise full route handler and response shaping logic
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/05-observability-hardening-and-testing/05-03-SUMMARY.md`
</output>
