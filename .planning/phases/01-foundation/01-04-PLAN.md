---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - packages/db/src/seed.ts
  - packages/db/src/seed-data.ts
autonomous: true
requirements:
  - FOUND-05

must_haves:
  truths:
    - "pnpm db:seed populates the database with 3 users (collector, flipper, new user)"
    - "Seeded cards exist in all 4 states: VAULTED, EXTERNAL, ON_MARKET, IN_TRANSIT"
    - "At least 50 cards across 3-4 IPs (pokemon, one-piece, sports, yugioh)"
    - "Price ranges vary from $5 to $5000 with different priceConfidence levels"
    - "2 packs with associated pack_cards mappings exist"
    - "Marketplace listing stubs exist"
    - "Running seed a second time does not create duplicates (idempotent)"
  artifacts:
    - path: "packages/db/src/seed.ts"
      provides: "Main seed script entry point"
      min_lines: 20
    - path: "packages/db/src/seed-data.ts"
      provides: "Seed data fixtures — users, cards, userCards, packs, listings"
      min_lines: 100
  key_links:
    - from: "packages/db/src/seed.ts"
      to: "packages/db/src/client.ts"
      via: "imports prisma client for database operations"
      pattern: "import.*prisma.*from"
    - from: "packages/db/src/seed.ts"
      to: "packages/db/src/seed-data.ts"
      via: "imports fixture data constants"
      pattern: "import.*from.*seed-data"
---

<objective>
Create comprehensive, idempotent seed data fixtures populating the database with realistic development data: 3 user personas, ~50 cards across multiple IPs, cards in all states, realistic price ranges, packs, and marketplace listing stubs. Verify the full build pipeline end-to-end.

Purpose: Seed data enables local development and testing for all subsequent phases. Without realistic data covering all card states and user scenarios, rules engine and agent development cannot iterate.
Output: `pnpm db:seed` populates the database reliably, and the full `pnpm install && pnpm build` pipeline succeeds.
</objective>

<execution_context>
@/Users/chaseguo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chaseguo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create seed data fixtures and idempotent seed script</name>
  <files>
    packages/db/src/seed-data.ts,
    packages/db/src/seed.ts
  </files>
  <action>
    **packages/db/src/seed-data.ts:**
    Define all seed data as typed constants with FIXED UUIDs (for idempotent upserts). Use UUID v7 format strings. Structure:

    3 Users:
    - "collector-user" — Alice, many cards, diverse IPs (pokemon, one-piece, sports)
    - "flipper-user" — Bob, few high-value cards (pokemon, yugioh), focused on market activity
    - "new-user" — Carol, 1-2 cards, minimal activity

    ~50 Cards across 4 IPs:
    - Pokemon (~15): Charizard VMAX, Pikachu V, Mewtwo GX, etc. Various sets and grades (PSA 10, PSA 9, BGS 9.5, RAW)
    - One Piece (~12): Luffy, Zoro, Nami leaders and characters. Japanese and English language mix.
    - Sports (~12): Baseball, basketball cards. Variety of grades.
    - Yu-Gi-Oh (~11): Blue-Eyes, Dark Magician, etc. Mix of graded and RAW.

    UserCards in ALL states (per locked decision):
    - VAULTED: ~20 cards (high-value, graded, in Gacha vault)
    - EXTERNAL: ~10 cards (user-uploaded, not in vault)
    - ON_MARKET: ~8 cards (listed on marketplace)
    - IN_TRANSIT: ~4 cards (being shipped to vault)

    Price data:
    - Range: $5 (common RAW) to $5,000 (mint Charizard)
    - priceConfidence distribution: LIVE (recently checked), RECENT_24H, STALE_7D, NO_DATA (for some external cards)
    - priceFetchedAt: various dates (some recent, some stale)

    ExternalCards:
    - 5-8 external cards owned by Alice and Bob
    - Some with certNumbers, some without
    - Varying priceConfidence levels

    Packs:
    - Pack 1: "Pokemon Obsidian Flames" (Alice, opened, 5 cards)
    - Pack 2: "One Piece OP-07" (Alice, unopened)

    PackCards:
    - Link Pack 1 to 5 specific cards

    MarketplaceListings:
    - 3-4 stubs: ACTIVE listings for ON_MARKET cards with list prices

    ActionsLog:
    - 2-3 sample entries showing past agent recommendations and user actions

    **packages/db/src/seed.ts:**
    Import prisma client and seed data. Use upsert for all records to ensure idempotency (Pitfall #5 — running seed twice must not create duplicates).

    Structure:
    1. Seed users (upsert by fixed id)
    2. Seed cards (upsert by fixed id)
    3. Seed userCards (upsert by fixed id)
    4. Seed externalCards (upsert by fixed id)
    5. Seed packs (upsert by fixed id)
    6. Seed packCards (upsert by fixed id)
    7. Seed marketplaceListings (upsert by fixed id)
    8. Seed actionsLog entries (upsert by fixed id)
    9. Log summary: "Seeded: N users, N cards, N userCards, N externalCards, N packs, N listings"

    Use prisma.$transaction for atomicity where possible. Wrap in main() with catch and finally ($disconnect).

    Ensure package.json seed script works: `"seed": "tsx src/seed.ts"`.
  </action>
  <verify>
    <automated>cd /Users/chaseguo/Documents/GitHub/tcg-agent && pnpm --filter @tcg/db build</automated>
  </verify>
  <done>seed-data.ts contains all fixture constants with fixed UUIDs. seed.ts is an idempotent script using upsert. TypeScript compiles without errors. When run against a live Postgres with the migration applied, `pnpm db:seed` populates all tables with 3 users, ~50 cards in all states, realistic prices, 2 packs, and marketplace stubs. Running seed a second time produces no duplicates or errors.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end build and seed verification</name>
  <files></files>
  <action>
    Run the complete Phase 1 verification pipeline to confirm everything works together:

    1. `pnpm install` — all dependencies resolve, workspace links work
    2. `pnpm build` — all 4 packages compile in correct order (schemas -> db -> agent -> api)
    3. `pnpm --filter @tcg/schemas test` — all schema unit tests pass
    4. `pnpm build --dry-run` — verify Turborepo task execution order shows schemas before db before agent before api
    5. If Postgres is available:
       a. `pnpm db:migrate` — migration applies cleanly (or is already applied)
       b. `pnpm db:seed` — seed script runs without error
       c. `pnpm db:seed` (second time) — verify idempotency, no duplicate errors

    Verify the complete Phase 1 success criteria from ROADMAP.md:
    - SC1: `pnpm install && pnpm build` succeeds across all four packages with no type errors
    - SC2: Migration produces all tables including user_cards state enum and price columns
    - SC3: Zod schemas importable from @tcg/schemas with nullable convention
    - SC4: Seed data populates all states and user scenarios
    - SC5: Turborepo build pipeline respects dependency order

    If any verification fails, diagnose and fix the issue. This is the final quality gate before the phase is complete.
  </action>
  <verify>
    <automated>cd /Users/chaseguo/Documents/GitHub/tcg-agent && pnpm install && pnpm build && pnpm --filter @tcg/schemas test</automated>
  </verify>
  <done>Full pipeline green: install, build (all 4 packages in order), schema tests pass. If Postgres available: migration applied, seed data populated with all card states and user scenarios, seed is idempotent. Phase 1 success criteria from ROADMAP.md are all met.</done>
</task>

</tasks>

<verification>
1. `pnpm db:seed` runs without error against a Postgres instance with migration applied
2. Running `pnpm db:seed` a second time does not produce duplicate errors (idempotent upserts)
3. Database contains 3 users, ~50 cards, userCards in all 4 states, externalCards, 2 packs, marketplace stubs
4. `pnpm install && pnpm build` succeeds end-to-end across all packages
5. `pnpm --filter @tcg/schemas test` passes all schema unit tests
6. Full Phase 1 success criteria from ROADMAP.md verified
</verification>

<success_criteria>
- Seed script populates: 3 users (collector/flipper/new), ~50 cards (4 IPs), all 4 card states, realistic prices ($5-$5000), varying priceConfidence, 2 packs, marketplace stubs
- Seed is idempotent — runs twice without error or duplicates
- Full build pipeline (install -> build -> test) passes
- Phase 1 ROADMAP success criteria all met
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
